#!groovy

node {
    
    stage 'Build'
        echo 'Building'

    stage 'Artifactory configuration'
       echo 'Configuring'

    stage 'Exec Maven'
       echo 'Execution'

    stage 'Publish build info'
        echo 'Publishing'
}
